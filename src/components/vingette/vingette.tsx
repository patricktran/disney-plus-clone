import styled from "styled-components/macro";

const Vingette = styled.div`
  background-image: radial-gradient(
    farthest-side at 73% 21%,
    transparent,
    rgb(26, 29, 41)
  );
  position: absolute;
  inset: 0px;
`;

export default Vingette;
